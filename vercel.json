{
    "builds": [{
        "src": "classic-builders-inc/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "classic-builders-inc/wsgi.py"
        }
    ]
}